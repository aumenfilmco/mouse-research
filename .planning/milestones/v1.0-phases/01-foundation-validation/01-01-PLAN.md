---
phase: 01-foundation-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mouse_research/__init__.py
  - src/mouse_research/cli.py
  - tests/__init__.py
autonomous: true
requirements:
  - SETUP-02

must_haves:
  truths:
    - "`pip install -e .` completes without errors"
    - "`mouse-research --help` prints usage and lists install/doctor/login commands"
    - "The src/mouse_research package is importable as a module"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependency list, entry point"
      contains: "mouse-research = \"mouse_research.cli:app\""
    - path: "src/mouse_research/__init__.py"
      provides: "Package marker"
    - path: "src/mouse_research/cli.py"
      provides: "Typer app with stub commands: install, doctor, login"
      exports: ["app"]
    - path: "tests/__init__.py"
      provides: "Test package marker"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "src/mouse_research/cli.py::app"
      via: "entry_points console_scripts"
      pattern: "mouse-research.*mouse_research.cli:app"
---

<objective>
Create the installable Python project scaffold: pyproject.toml with all declared dependencies, the src/ layout, a Typer CLI entry point with stub commands, and a tests/ package. Running `pip install -e .` must complete and `mouse-research --help` must work.

Purpose: Every subsequent plan depends on this scaffold existing. Nothing can be built until the package is pip-installable and the CLI entry point is wired.
Output: A pip-installable Python package with a working `mouse-research` command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-validation/01-RESEARCH.md
@.planning/phases/01-foundation-validation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with full dependency list and entry point</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-foundation-validation/01-RESEARCH.md (Pattern 1 section — exact pyproject.toml template with dependency versions)
    - CLAUDE.md (Technology Stack section — canonical version pinning)
  </read_first>
  <action>
Create pyproject.toml at the project root /Users/aumen-server/Projects/researchpapers/pyproject.toml with this exact content:

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mouse-research"
version = "0.1.0"
description = "CLI-driven newspaper research pipeline for the MOUSE documentary project"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.24.1",
    "rich>=14.3.3",
    "pydantic-settings>=2.13.1",
    "playwright>=1.58.0",
    "ollama>=0.6.1",
    "pytesseract>=0.3.13",
    "Pillow>=12.2.0",
    "httpx>=0.28.1",
    "opencv-python>=4.13.0",
    "newspaper4k>=0.9.5",
    "trafilatura>=2.0.0",
    "python-frontmatter>=1.1.0",
]

[project.scripts]
mouse-research = "mouse_research.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/mouse_research"]

[tool.pytest.ini_options]
testpaths = ["tests"]

Note: httpx pinned as >=0.27.0 (not 0.28.1) — that specific version had MEDIUM confidence per RESEARCH.md. All other versions match CLAUDE.md exactly.
  </action>
  <verify>
    <automated>cd /Users/aumen-server/Projects/researchpapers && python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['scripts']['mouse-research']=='mouse_research.cli:app'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at project root
    - grep finds: mouse-research = "mouse_research.cli:app"
    - grep finds: requires-python = ">=3.11"
    - grep finds all 12 dependency packages (typer, rich, pydantic-settings, playwright, ollama, pytesseract, Pillow, httpx, opencv-python, newspaper4k, trafilatura, python-frontmatter)
    - grep finds: packages = ["src/mouse_research"]
  </acceptance_criteria>
  <done>pyproject.toml exists with correct entry point, all 12 Python dependencies declared, and hatchling build backend configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/mouse_research package with Typer CLI stub</name>
  <files>
    src/mouse_research/__init__.py
    src/mouse_research/cli.py
    tests/__init__.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-validation/01-RESEARCH.md (Pattern 2: Typer Multi-Command App — exact cli.py template)
    - pyproject.toml (just written — verify entry point target matches)
  </read_first>
  <action>
Create the following three files:

--- src/mouse_research/__init__.py ---
"""MOUSE Research Pipeline — CLI-driven newspaper research tool."""
__version__ = "0.1.0"

--- src/mouse_research/cli.py ---
"""Typer CLI entry point for mouse-research."""
import typer
from rich.console import Console

app = typer.Typer(no_args_is_help=True, help="MOUSE newspaper research pipeline")
console = Console()


@app.command()
def install():
    """Install Node.js dependencies (newspapers-com-scraper)."""
    console.print("[yellow]install command not yet implemented — run after plan 01-03[/yellow]")


@app.command()
def doctor():
    """Validate all external dependencies and report status."""
    console.print("[yellow]doctor command not yet implemented — run after plan 01-04[/yellow]")


@app.command()
def login(domain: str = typer.Argument(..., help="Domain to authenticate (e.g. newspapers.com)")):
    """Open browser for manual login and save cookies."""
    console.print(f"[yellow]login command not yet implemented — run after plan 01-05[/yellow]")


if __name__ == "__main__":
    app()

--- tests/__init__.py ---
(empty file — package marker only)

Then run pip install -e . from the project root:
  cd /Users/aumen-server/Projects/researchpapers && pip3 install -e .

This installs the package in editable mode so subsequent plans can import mouse_research directly.
  </action>
  <verify>
    <automated>cd /Users/aumen-server/Projects/researchpapers && pip3 install -e . --quiet && mouse-research --help</automated>
  </verify>
  <acceptance_criteria>
    - src/mouse_research/__init__.py exists and contains __version__ = "0.1.0"
    - src/mouse_research/cli.py exists and contains: app = typer.Typer(
    - src/mouse_research/cli.py contains three @app.command() decorators: install, doctor, login
    - tests/__init__.py exists (may be empty)
    - `pip3 install -e .` exits with code 0
    - `mouse-research --help` output contains "install", "doctor", "login"
    - `python3 -c "from mouse_research.cli import app"` exits with code 0
  </acceptance_criteria>
  <done>`mouse-research --help` shows install/doctor/login commands. `python3 -c "from mouse_research.cli import app"` imports without error.</done>
</task>

</tasks>

<verification>
After both tasks complete:
  cd /Users/aumen-server/Projects/researchpapers && mouse-research --help

Expected: Usage line, help text "MOUSE newspaper research pipeline", and three commands listed: install, doctor, login.
</verification>

<success_criteria>
- `pip install -e .` completes with exit code 0
- `mouse-research --help` prints usage with install/doctor/login commands
- `python3 -c "import mouse_research; print(mouse_research.__version__)"` prints 0.1.0
- All 12 Python dependencies declared in pyproject.toml
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-validation/01-01-SUMMARY.md`
</output>
